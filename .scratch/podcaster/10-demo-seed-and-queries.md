# 10 — Demo seed + search queries

**Triage:** ready-for-agent

## What to build

Make the app demo-ready with enough varied, realistic content for search, filters, and
recommendations to look real.

- **Hybrid seed:** ~5–6 fully generated "hero" podcasts with real audio you'd actually play in
  a walkthrough, plus ~15 metadata + real-embedding entries so semantic search, filters, and
  recommendations have enough varied volume.
- Prepared **semantic-search demo queries** that show meaning-based matching beating keyword
  matching.

## Acceptance criteria

- [ ] ~5–6 hero podcasts seeded with real audio + thumbnail + embedding
- [ ] ~15 additional entries seeded with metadata + real embeddings
- [ ] A documented set of demo queries demonstrating meaning-based matches over keyword matches

## Blocked by

- 03 — Real Gemini generation + custom thumbnail upload
- 07 — Discover: semantic search + category filter
