node_modules
.DS_Store
dist
dist-ssr
.vercel
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Local agent state (worktrees, node_modules, local settings)
.claude/

# Personal / draft write-ups (keep local only)
docs/writeups/

# npm-only project — do not commit pnpm artifacts
pnpm-lock.yaml
pnpm-workspace.yaml
